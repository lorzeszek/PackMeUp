<?xml version="1.0" encoding="utf-8" ?>
<toolkit:Popup
    x:Class="PackMeUp.Popups.PackingItemsPopup"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
    CanBeDismissedByTappingOutsideOfPopup="False">

    <VerticalStackLayout Padding="20" Spacing="15" BackgroundColor="White">

        <Label 
            Text="Proponowana lista"
            FontSize="18"
            FontAttributes="Bold"
            HorizontalOptions="Center" />

        <Label 
            Text="Wybierz rzeczy do dodania"
            HorizontalOptions="Center" />

        <!-- Select / Deselect -->
        <HorizontalStackLayout Spacing="10" HorizontalOptions="Center">

            <Button Text="Zaznacz wszystko" Clicked="OnSelectAllClicked"/>
            <Button Text="Odznacz wszystko" Clicked="OnDeselectAllClicked"/>

        </HorizontalStackLayout>

        <!-- Lista -->
        <CollectionView ItemsSource="{Binding Items}" HeightRequest="300">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <HorizontalStackLayout Spacing="10">
                        <CheckBox IsChecked="{Binding IsSelected}" />
                        <Label Text="{Binding Name}" VerticalOptions="Center"/>
                    </HorizontalStackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <!-- Przyciski -->
        <HorizontalStackLayout Spacing="10" HorizontalOptions="Center">

            <Button Text="Anuluj" Clicked="OnCancelClicked" />

            <Button Text="Dodaj" Clicked="OnConfirmClicked" />

        </HorizontalStackLayout>

    </VerticalStackLayout>
</toolkit:Popup>