<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="PackMeUp.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:PackMeUp"
    FlyoutBehavior="Disabled"
    xmlns:views="clr-namespace:PackMeUp.Views"
    Title="PackMeUp"
    Shell.TabBarBackgroundColor="{AppThemeBinding Light=#F5F5F5, Dark=#1E1E1E}"
    Shell.TabBarTitleColor="Red"
    Shell.TabBarUnselectedColor="{AppThemeBinding Light=#888888, Dark=#888888}"
    >

    <Shell.TitleView>
        <views:ShellHeaderView />
    </Shell.TitleView>

        

    <!--<TabBar>

        <Tab Title="Home" Icon="home.png">
            <ShellContent
                Route="Home"
                ContentTemplate="{DataTemplate views:StartPage}" />
        </Tab>

        <Tab Title="Trips" Icon="list.png">
            <ShellContent
                Route="TripList"
                ContentTemplate="{DataTemplate views:TripListPage}" />
        </Tab>

        <Tab Title="Weather" Icon="weather.png">
            <ShellContent
                Route="Weather"
                ContentTemplate="{DataTemplate views:WeatherPage}" />
        </Tab>

        <Tab Title="Docs" Icon="docs.png">
            <ShellContent
                Route="Docs"
                ContentTemplate="{DataTemplate views:DocsPage}" />
        </Tab>

        <Tab Title="Settings" Icon="settings.png">
            <ShellContent
                Route="Settings"
                ContentTemplate="{DataTemplate views:SettingsPage}" />
        </Tab>

    </TabBar>-->
    <TabBar>

        <ShellContent
            Title="Home"
            Icon="home.png"
            Route="Home"
            ContentTemplate="{DataTemplate views:StartPage}" />

        <ShellContent
            Title="Trips"
            Icon="travel_bag.png"
            Route="TripList"
            ContentTemplate="{DataTemplate views:TripListPage}" />

        <ShellContent
            Title="Weather"
            Icon="cloudy.png"
            Route="Weather"
            ContentTemplate="{DataTemplate views:WeatherPage}" />

        <ShellContent
            Title="Docs"
            Icon="archive.png"
            Route="Docs"
            ContentTemplate="{DataTemplate views:DocsPage}" />

        <ShellContent 
            Title="Settings" 
            Icon="settings.png"
            Route="Settings"
            ContentTemplate="{DataTemplate views:SettingsPage}" />

    </TabBar>

</Shell>
